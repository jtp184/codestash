<% case params[:kind] %>
<% when 'text' %> 
	<%= simple_form_for :stash_element do |f| %>
		<%= f.input :text_content, as: :text%>
	<% end %>
<% when 'sms' %> 
	<%= simple_form_for :stash_element do |f| %>
		<%= f.input :text_content, as: :text%>
	<% end %>
<% when 'location' %> 
<% when 'photo' %>
	<%= simple_form_for :stash_element do |f| %>
		<%= f.input :file, as: :file %>
	<% end %> 
<% when 'file' %>
	<%= simple_form_for :stash_element do |f| %>
		<%= f.input :file, as: :file %>
	<% end %> 
<% when 'link' %>
	<%= simple_form_for :stash_element do |f| %>
		<%= f.input :text_content, as: :text%>
	<% end %>
<% end %>
<%= link_to "Cancel", {:action => "new_post"} %>
<%= link_to "Add", {:action => "new_post"} %>