<%= link_to (image_tag (asset_path "back_invert.png"), size: 20), controller: 'code_lookup', action: 'show', cid: @stash.code.id %>
<% @elements.each do |element| %>
<%= render "generic", stash_element: element %>
<% end %>
