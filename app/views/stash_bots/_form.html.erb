<%= simple_form_for(@stash_bot) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :secret_code %>
    <%= f.input :prefs %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
